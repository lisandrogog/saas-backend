export enum DocumentStatus {
  DRAFT = 'draft',
  PENDING = 'pending',
  SCHEDULED = 'scheduled',
  IN_PROGRESS = 'in_progress',
  CANCELED = 'canceled',
  COMPLETED = 'completed',
  VOIDED = 'voided',
}
